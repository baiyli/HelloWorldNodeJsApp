version: 1.0
runtime: nodejs12
build:
  commands:
    build:
      - echo $CODEBUILD_BUILD_IMAGE
      - npm install
run:
  runtime-version: 12.22.12
  command: node index.js
  network:
    port: 3000
    env: APP_PORT
